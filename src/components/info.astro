---
import { Picture } from "astro:assets";
import tableImage from "assets/fullimage.png";
import Link from "@components/ui/link.astro";
import { Icon } from "astro-icon/components";
import Container from "./container.astro";
---

<main class="relative overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">
    <div class="flex flex-col items-center py-8 md:py-16">
      <!-- Imagen a la izquierda en desktop -->
      <div class="w-full md:w-1/2 -mb-4 md:mb-0 order-1">
        <Picture
          src={tableImage}
          alt="Mesa con comida deliciosa"
          widths={[400, 768, 1024, 1440]}
          sizes="(max-width: 768px) 100vw, 50vw"
          loading="eager"
          format="avif"
          class="w-full max-w-md mx-auto h-auto object-cover"
        />
      </div>

      <!-- Texto a la derecha en desktop -->
      <div
        class="w-full md:w-1/2 pt-4 md:pt-16 pb-4 md:pb-16 order-2 text-center md:text-left">
        <h1
          class="text-title text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-black leading-tight mt-4">
          Sabores <br /> auténticos,<br /> más ricos
        </h1>
        <hr class="h-px border-0 my-8 w-10 bg-primary mt-5" />
        <p class="mt-6 text-lg text-title leading-relaxed">
          Llenos de <span class="text-primary font-medium">sabor</span> y preparados
          con ingredientes de la
          <span class="text-primary font-medium">más alta calidad</span>,
          nuestras especialidades son ideales para cualquier ocasión.
        </p>
        <p class="mt-4 text-lg text-title leading-relaxed">
          Es por esto que recomendamos que sorprendas a tus invitados, con
          nuestros productos en cualquier tipo de evento.
        </p>
        <a
          href="/about"
          class="inline-block mt-10 px-6 py-3 text-base font-medium text-white bg-[#F4B223] hover:bg-[#E5A420] rounded-lg transition-colors">
          Más sobre KOOK
        </a>
      </div>
    </div>
  </div>
</main>

<style>
  @media (min-width: 768px) {
    .flex-col {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }
  }
</style>
